 void slectsort(int r[], int a)
{
	int i, j, temp;
	for(i=1; i<a; i++)
	{
		temp = i;
		for(j=i+1; j<=a; j++)
		{
			if(r[temp] < r[j])
				temp = j;
		}
		r[0] = r[i];
		r[i] = r[temp];
		r[temp] = r[0];
	}
}
